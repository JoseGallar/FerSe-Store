<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Ferse"
        tools:targetApi="31">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.view.AddProductActivity"
            android:label="Nuevo Producto"
            android:parentActivityName=".MainActivity"/>

        <activity android:name=".ui.view.ProductDetailActivity"
            android:label="Detalle del Producto"
            android:parentActivityName=".MainActivity"/>

        <activity android:name=".ui.view.WalletActivity"
            android:label="Mi Billetera"
            android:parentActivityName=".MainActivity"/>

        <activity android:name=".ui.view.CalculatorActivity"
            android:label="Calculadora de Costos"
            android:parentActivityName=".MainActivity"/>

        <activity android:name=".ui.view.FinancialActivity"
            android:label="Movimientos"
            android:parentActivityName=".ui.view.WalletActivity"/>

    </application>

</manifest>